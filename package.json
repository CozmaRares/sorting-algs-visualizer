{
  "name": "sorting-algs-visualizer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:domain": "echo sorting.raru.dev > ./dist/CNAME",
    "deploy": "npm run build && npm run deploy:domain && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "p5": "^1.7.0"
  }
}
